'use strict'

module.exports = {
  beforeEach: function (browser, done) {
    // Do stuff before each test, e.g.
    // require('nightwatch-video-recorder').start(browser, done)
  },
  afterEach: function (browser, done) {
    // Do stuff after each test.
  }
}
